name: injectable_codegen
description: Code generator for injectable supporting boundary-aware folder micro-packages, nested feature modules, and compositors.
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  analyzer: ">=6.0.0 <15.0.0"
  build: ">=2.4.0 <5.0.0"
  code_builder: ^4.10.0
  glob: ^2.1.0
  injectable:
  path: ^1.9.0
  source_gen: ^4.1.1

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.25.6
