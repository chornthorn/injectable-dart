name: micro_package_example
description: Demonstrates folder-based nested micro-packages with injectable without dedicated pubspec.yaml files.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  dio: ^5.8.0+1
  get_it: ^9.2.1
  injectable:

dev_dependencies:
  build_runner: ^2.4.0
  injectable_codegen:
  lints: ^6.0.0
  test: ^1.25.6
