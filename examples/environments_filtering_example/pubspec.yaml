name: environments_filtering_example
description: >-
  Demonstrates environment gating (@Injectable(env: ...) / @Environment(...))
  and environment filters (built-in and custom) with injectify.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  get_it: ^9.2.1
  injectify:

dev_dependencies:
  build_runner: ^2.4.0
  injectify_generator:
  lints: ^6.0.0
  test: ^1.25.6