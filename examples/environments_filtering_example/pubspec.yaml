name: environments_filtering_example
description: >-
  Demonstrates environment gating (@Injectable(env: ...) / @Environment(...))
  and environment filters (built-in and custom) with injectable.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  get_it: ^9.2.1
  injectable:

dev_dependencies:
  build_runner: ^2.4.0
  injectable_codegen:
  lints: ^6.0.0
  test: ^1.25.6